[pytest]
testpaths = tests
addopts =
    --tb=short
    --strict-markers
    -q
    --cov=.
    --cov-report=term-missing
    --cov-report=html:htmlcov
    --cov-omit=tests/*,static/*,templates/*

markers =
    slow: marks tests that make real network calls (deselect with -m "not slow")
